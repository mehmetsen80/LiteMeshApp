## lite-gateway-service.yml
#apiVersion: v1
#kind: Service
#metadata:
#  name: api-gateway
#  labels:
#    app: api-gateway
#spec:
#  type: ClusterIP
#  selector:
#    app: api-gateway
#  ports:
#    - protocol: TCP
#      port: 80           # Internal port for other services in the cluster
#      targetPort: 7777   # The port exposed by the container
